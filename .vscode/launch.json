{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Attach (Dev Container)",
      "type": "coreclr",
      "request": "attach",
      "processId": 1,
      "requireExactSource": false,
      "sourceFileMap": {
        "/app/backend": "${workspaceFolder}"
      }
    }
  ]
}
